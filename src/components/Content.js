import React from 'react';
import Tappable from 'react-tappable';

const Content = React.createClass({

  render() {
    return (
      <div style={{ background: "#B4D2E7", color: "green", width: "400px", height: "300px", }}>
      <h2 style={{ textAlign: "center" }}>RAVEMOM</h2>
    </div>
    );
  },
});

module.exports = Content;
